#include "main.h"
#include <stdio.h>

/* Function Prototype */
char *_strchr(char *s, char c);
/* Data Segment */


/* BSS Segment */


/* Stack */
char *_strchr(char *s, char c)
{
		int a;

		while (1)
		{
			a = *s++;
			if (a == c)
			{
				return (s - 1);
			}
			if (a == 0)
			{
				return (NULL);
			}
		}
}

/* Text Segment */
int main(void)
{
    char *s = "hello";
    char *f;

    f = _strchr(s, 'l');

    if (f != NULL)
    {
        printf("%s\n", f);
    }
    return (0);
}
